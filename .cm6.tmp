fix: 逐条刁钻自查 —— 修 4 处真问题（v1.7.0）

峰宝要求「逐一质问自己，构造特殊的刁钻的真实测试用例，每个检查点能防住吗」。
分三批实测，**找到 4 个真问题**（1 个漏洞 + 3 个规则精度）。

第一批 · 证据门禁深度攻击（8 种）
--------------------------------------------------
沿用上一轮的 14 种后又加了 8 种更刁的。**发现真漏洞 E1/E4**：

    total()                       # 调用了（探针记录到 → touches 满足）
    print("总价: 42.0")            # 但输出是硬编码的，没经过它

上一轮只防到「import 但不调用」。这一层是「调用但丢弃结果」——
探针看到调用就放行，可输出与它无关。
**修法**：探针同时记录**函数返回值**，校验时要求「返回值必须出现在输出里」。
真返回 0.0、输出却是 42.0 → 脱节 → 拦。
（副产品：E2 子进程 / E3 多线程因 settrace 边界而**拦**，方向安全；
E5 路径变体 `./src/cart.py` 正确规范化、无误拦。）

第二批 · guard 11 条规则逐条双向对照（35 样本）
--------------------------------------------------
**修掉 3 处**：
1. `not-implemented` **误报**：`if x is NotImplemented:` / `return NotImplemented`
   是**合法的 Python 比较协议写法**（实现 __eq__ 时返回它让解释器尝试反向比较），
   却被当成桩。移除独立的 `NotImplemented` 分支。
2. `stub-return` **误报**：`return "mockup design"` 里 mockup 是正常设计术语。
   加负向前瞻 `mock(?![a-zA-Z])`。
3. `stub-variable` **漏报**：`def get_mock_data():` 抓不到 ——
   `\b` 在 `_m` 之间不成立（`_` 也是 word char）。改用 `(?<![a-zA-Z])`。

修后 35/35 全通过（另 3 项是描述性文字 vs 标记的边界，需人工判断）。

第三批 · 其他模块关键盲区（6 项）
--------------------------------------------------
- **循环引用**（alpha↔beta 互 import 且都没接主流程）→ **检出了**（走入口可达性）
- 动态 import（importlib）→ 检出（静态看它孤立，方向保守）
- 自定义断言辅助函数（assert_that）→ 正确判 **S1**（未误判为无断言）
- 分支内断言 / 空期望串 → 正确处理

回归：14 种造假手法套件 12/12 · 模板 lint 0E/0W · ruff All checks passed

CAPABILITIES.md 已同步：guard 从「9 条未验证」升级为「11 条已逐条双向对照」，
并记录这 4 处修复的判据与理由。

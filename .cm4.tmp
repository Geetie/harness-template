feat(wiring): 接线检查真正生效 —— 未集成代码现在会被拦（v1.6.0）

峰宝问「未集成接线你是如何拦截的？」—— 实测答案是：此前根本拦不住
---------------------------------------------------------------
干净地单独测「一个完整实现但没有任何非测试代码引用它」的模块：

  反占位符 guard / quality / test_audit / harness_lint -> 全部放过
  check_integration -> 根本没跑（脚本不在 standard 档）
  pre-commit -> 拦住，但原因是「代码规范」，与接线无关（歪打正着）

三个叠加的原因：
  1. check_integration.py 属 integration-check 模块，standard 档不含它
  2. 它探测不到主流程入口就直接 exit 2 —— 而新项目默认没配 entry
  3. 于是 pre-commit 检查 5 永远走「未配置 entry，跳过」分支
  => 一个只在理想条件下工作的门禁，等于没有门禁

修法
---------------------------------------------------------------
新增「入度=0」降级判定（不依赖入口）：一个源码文件若没有被任何其他
源码文件 import，它要么是入口，要么没人用。这个判断不需要知道入口在哪，
正好补上「探测不到入口就完全不检查」的洞。

配套四条：
· 入口豁免 looks_like_entry：工具脚本目录（scripts/ tools/ bin/）/
  含 `if __name__ == "__main__"` / 入口候选名 —— 三类都豁免
· 公共排除清单 is_infra_file（__init__.py 等基建不再被误报为孤儿）
· integration-check 进 standard 档（未集成是通用问题，不该只在 full 档）
· pre-commit 检查 5 改为默认执行（配了 entry 走可达性，没配走降级）
· --allow 白名单（config.json 的 integration.allow_orphans）

修的过程中踩到的两个坑（都写进注释了）
---------------------------------------------------------------
· 降级判定最初写「全部入度 0 就不判断」→ 真孤儿漏报。
  多模块项目里「全部入度 0」恰恰最该报（各写各的）。
  改成「候选模块 < 2 个才不判断」。
· 「入口豁免」最初只按文件名匹配 → 模板仓库自身 16 个 CLI 脚本
  被误报 8 个孤儿。补上「工具脚本目录 + __main__ 标记」后归零。

验证（双向）
---------------------------------------------------------------
· 正向：模板自身 16 个工具脚本 → 0 孤儿（无噪音）
· 反向：真实项目里未被引用的 reporting/exporter.py → 检出并阻断
· 端到端：真实 git commit 被 pre-commit 拦下，明确列出「接线检查未通过」
· 模板 lint 0E/0W · ruff All checks passed

已知局限（诚实记录）
---------------------------------------------------------------
降级判定只找「没人 import 的文件」。「注册了但不触发」「触发了但写到
没人读的地方」它抓不到 —— 那两类需行为验证（同 evidence_gate 的思路）。
import 解析失败时会给 WARN 说明可能误判，不假装精确。

feat(lint): 新增 L012 清单自洽 —— 把「模块清单不变式」变成机器校验（v1.13.0）

第十一批：先证伪了我自己上一条报告的说法
--------------------------------------------------
上轮报告我写「两份映射内容已不一致：decisions 4 vs 2、routing 4 vs 1…」——
**那是误判**。实测后发现：

  · `MODULES[mid]["owned"]`  = 模块拥有的**全部**文件（复制过滤用）
  · `MODULE_REQUIRED[mid]`  = **体检红线**（"少这份就算这层不完整"，故意精简）

**语义本就不同，不是重复。** 我此前把"数量不等"当成了"漂移"。
实测 `required ⊆ owned` **全部满足**，没有漂移。

正确动作：不是合并，而是加机器校验
--------------------------------------------------
新增 `module_manifest.self_check(root)`，校验 5 类不变式：

  1. **同一文件不被两个模块 own** ← 这条对应上一批的真实事故
  2. `required ⊆ owned`
  3. 所有声明路径存在于磁盘
  4. 每个模块 owned 非空
  5. deps 引用的模块确实存在

第 1 条最要紧：上一批 `sync_lib.py` 同时出现在 verification 与 upgrade 的
owned 里，因生成侧「**排除优先于包含**」→ minimal/standard 档的
`new_project.py` 一跑就 `ModuleNotFoundError`。
**清单自己矛盾，却没有任何地方检查过** —— 现在有了。

接入 lint：新增规则 **L012 清单自洽**（error 级）
--------------------------------------------------
放在主流程（不进 decision_checks 组）—— 它不依赖任何可选模块，
清单文件缺失时规则内部会明确跳过。

验证（双向 5/5）
--------------------------------------------------
· 基线（清单自洽）→ 0 报 ✅
· ① 制造双归属 → **检出**（正好复现上一批的事故）✅
· ② required ⊄ owned → 检出 ✅
· ③ 声明幽灵路径 → 检出 ✅
· ④ 恢复清单 → 回到 0 报（无残留）✅
· ruff All checks passed · init 42/42

又一次「同名锚点误命中」（累计第 11 次方法论问题）
--------------------------------------------------
插 L012 调用时，我的锚点 `findings += check_l011(root, bodies)` 命中了
`if decision_checks:` 块**内部**的那一处，而不是主分发处 ——
破坏了 if/else 结构引发 IndentationError。

**规律**：同一段代码在文件里出现多次（或格式相似）时，单行锚点不可靠。
上一批（module_manifest 双映射）是同一类问题的另一面。
**锚点必须带唯一上下文**（前后行 / 缩进层级 / 所属函数）。

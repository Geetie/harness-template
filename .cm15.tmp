fix: 首次走查"用户第一天" —— 修 5 个第一天就踩的问题（v1.16.0）

峰宝问「该怎么使用这个 harness 模板？从 git clone 开始」。
我不凭记忆写手册，而是**真跑一遍** clone → 生成 → init，
结果发现 5 个用户第一天必然遇到的问题。

① pytest 误收集（最严重：所有 Python 项目第一天就红）
--------------------------------------------------
实测：生成的项目跑 `pytest` 报
  ERROR at setup of test_command
  file scripts/test_runner.py, line 76
  E   fixture 'cfg' not found

根因：模板自带的 `scripts/test_runner.py` / `scripts/test_audit.py`
文件名匹配 pytest 的 `test_*.py` 收集规则，里面的**业务函数**
（`test_command(cfg)`）被当成测试用例。
→ **任何用本模板的 Python 项目，跑 pytest 都会失败**，与用户代码无关。

修法：stack=python 时生成 `pytest.ini`（norecursedirs 排除 scripts/）。
选它而不是改 test 命令，因为**用户手跑 `pytest` 也正确**。
双向验证：无 ini → fixture 错误；有 ini → `1 passed`。

② `.py` 文件里的注释被机械替换（语义破坏）
--------------------------------------------------
实测：`sync_lib.py` 的 docstring
  「为什么必须有它：模板里是 {{PROJECT_NAME}}，项目里是实值。」
在生成的项目里变成
  「为什么必须有它：模板里是 <项目名>，项目里是实值。」
—— 原意是"模板侧是占位符形态、项目侧是实值"，
替换后成了"**模板里是 FreshApp**"，**语义完全反了**。

修法：`NO_SUBSTITUTE_EXTS = (".py",)`（与 `new_project.py` 同族理由），
生成侧与同步侧共用 `sync_lib.is_no_substitute`（单一真相源）。
验证：生成的 `sync_lib.py` 与模板**哈希完全一致**，注释保住、不含项目名。

③ 待填清单混入描述性文本
--------------------------------------------------
同一个根因的副作用：脚手架把 `{{KEY}}` 列进"请人工填写"清单，
用户会困惑"这个脚本我要填什么？"。修 ② 后清单从 16 项降为 **15 项全真待填**。

④ init 的报错信息不明确
--------------------------------------------------
`tail = (r.stdout or r.stderr ...)` —— **短路**导致 stdout 非空时
完全忽略 stderr，真实错误被吞。且不区分「命令不存在」与「命令失败」。
修：合并两个流；命令不存在（127/9009/`not recognized`/`No module named`）
判为"依赖未安装"**不算失败**（与 check_quality_tools 的既有原则一致：
"工具没装不该让 init 报红，否则新 clone 的仓库永远不健康"），并给安装提示。

⑤ `.cm14.tmp` 脏数据泄漏
--------------------------------------------------
我给上一次提交写 message 时的临时文件被 git 跟踪，并被复制进每个新项目。已清理。

效果
--------------------------------------------------
空项目 `python scripts/init.py`：**从 1/38 未通过 → 全部 38 项通过**。
生成输出也从"有噪音的 16 项待填"变成"15 项全真待填"。

回归：ruff All checks passed · 模板 lint 0E/0W
fix(pre-commit): 修正检查 1 的提示文案 —— 它与实现矛盾（v1.11.0）

第九批刁钻自查（evidence_gate 残留 + pre-commit 检查 1）
--------------------------------------------------
**通过的部分**（说明这些防线是扎实的）：
· A 结构防线 5/5：evidence 是数字 / 是数组 / 对象缺 cmd /
  expect_stdout 是字符串 / touches 是字符串 → 全部拦住
· B 强度锁 2/2：completed 从 2→1、删条目 → 都拦
· C 命令异常 2/2：cmd 指向不存在的脚本、cmd 是无效命令 → 都拦
· D feature_list.json 损坏 → exit 2（正确区分"结构错误"）

**发现真问题（本轮唯一修复）：检查 1 的提示与实现矛盾**
- docstring 写「progress.md **与** lessons.md 必须同步更新」（设计如此）
- 但 `hint` 文案写「若本次踩了新坑则更新 lessons.md」（**条件性**）
- 而代码是 `missing = [m for m in mandatory if m not in staged]`
  → **无条件**要求两个都进 staged

矛盾后果：用户按提示以为"没踩坑就不用改 lessons"，
提交时却被拦，只会觉得门禁莫名其妙。
**说明与实现不一致，比门禁严格本身更伤信任。**

修法：把 hint 改成与实现一致 ——
「progress.md 追加一行变更日志；lessons.md 也需同 commit 更新 ——
  本次没有新教训就写一行『本次无新教训』，不要留空不改」

（不动设计：docstring 的"两个都强制"是有意的，只是说明没跟上。）

验证（3/3）：
· 只更 progress（缺 lessons）→ 拦住，且提示里出现"不要留空不改" ✅
· 两个都更 → 放行 ✅
· 提示与实际要求一致 ✅

元教训（本轮又踩 1 次，累计第 7 次）：
第一版测试里我的基线 commit 自己被 pre-commit 拦了（没加 HARNESS_SKIP），
导致 progress.md 在第二次提交才落库 → 状态污染 →
得出"改代码不更 progress 也能过"的错误结论。
**夹具自身的初始状态必须先确保成功，否则后续全部结论不可信。**

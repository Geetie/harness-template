fix(manifest): sync_lib 归错模块 —— 导致 minimal/standard 档 new_project.py 崩溃（v1.12.0）

第十批：档位 × 语言栈 交叉矩阵（3×3 = 9 个组合）
--------------------------------------------------
之前从没系统测过这个维度。跑完发现**系统性问题**：

  minimal/standard 档生成的项目里，`scripts/new_project.py` 一跑就
  `ModuleNotFoundError: No module named 'sync_lib'`

根因：`sync_lib.py` 归在 `upgrade` 模块（**仅 full 档**），
但它是 `new_project.py` 的**必需依赖**（要用 `SYNC.snapshot` 写生成基线），
而 `new_project.py` 在**每个档位**都被复制。
→ **模块归属错误**：把"基础依赖"放进了可选模块。

修法：`sync_lib.py` 从 `upgrade` 移到 `verification`
（与 `module_manifest.py` 同处 —— 二者都是"生成/校验基础设施"）。

关键细节：**排除优先于包含** —— 文件只要出现在任何"已关闭模块"的 owned 里
就会被排除，所以不能"两边都放"，必须从 upgrade 一侧移走。

修后：9 个组合 × 全部脚本 = **0 崩溃**。

过程中的重要教训：我第一次改错了位置
--------------------------------------------------
`module_manifest.py` 里有**两份格式相似的映射**：
  · `MODULES[mid]["owned"]`（多行）—— **真正的复制依据**（new_project/sync_template 都用它）
  · `MODULE_REQUIRED[mid]`（单行）—— 另一用途

我写的替换锚点 `"upgrade": ["scripts/sync_template.py", "scripts/sync_lib.py"],`
**恰好只匹配 MODULE_REQUIRED 的单行格式** —— 于是改错了地方：
改完自以为修好了，重跑矩阵**仍然崩**，才发现真正的 owned 没动。

**这就是 P21「单一真相源」被违反的代价**：同名信息存两份 → 改一份看似成功、
行为不变 → 只能靠"改完必须重测"才发现。
（两份映射目前内容并不一致：decisions 4 vs 2、routing 4 vs 1、
 verification 12 vs 2、upgrade 2 vs 1 …… 值得后续统一。）

顺带发现：`new_project.py` **不在任何模块的 owned 里**（未归类），
所以它在所有档位都会被复制 —— 这也是它能崩到 minimal 档的原因。

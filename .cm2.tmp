fix(gate): 证据门禁加运行时验证 —— 挡住「import 但不调用」的假证据

对抗测试（站在想蒙混的 Agent 一侧）发现真漏洞
--------------------------------------------------
上一轮只测了 3 种造假手法。这次穷举 14 种，其中 A6 绕过了门禁：

    import src.cart          # 只为让 touches 的文本匹配通过
    print("总价: 42.0")       # 硬编码正确答案，根本没调用

根因：check_touches 只做**静态文本匹配**（"文本里有没有出现这个名字"）。
静态匹配永远可以绕（getattr / 字符串拼名 / 变量间接调用）。

修法一：运行时探针（scripts/_evidence_probe.py）
--------------------------------------------------
用 sys.settrace 记录执行轨迹，记「文件::函数名」而不是只记文件 ——
因为 `import src.cart` 本身就会触发模块级执行（co_name == "<module>"），
只记文件的话"import 但不用"会被误判成"执行过"。

判定规则：touches 的文件里必须有**具名函数**被调用；
仅有 <module> 时，若该文件含函数定义则判为"只 import 没调用" → 拦。

非 `python xxx.py` 形式的命令（pytest/node/go test）降级为静态检查，
并在输出里明确标注"未做运行时验证"，不假装验过。

修法二：产物指纹（挡复用旧文件）
--------------------------------------------------
原来只检查 artifacts "存在且非空" —— 预先放个旧文件就能满足。
现在记录执行前后的 size+mtime，完全没变即判为"不是本次产出"。

对抗测试结果（14 种手法）
--------------------------------------------------
拦住 13 种：无证据 / 通用词期望 / 空期望 / 期望值不符 / 不引用 /
  假引用(import不用) / cmd非脚本 / 假产物 / 空产物 / 假touches /
  降分母 / 双向都假 / 复用旧产物

放过 1 种（真边界，诚实记录）：
  A13 实现返回值恰好等于期望（def total(): return 42.0）+ 真调用
  → 门禁验证「行为与期望一致」，看不出实现是硬编码。
    这不是造假（行为确实正确），但业务上可能错 —— 只能靠人看代码。

回归：模板 lint 0E/0W · ruff All checks passed · 探针已注册进 manifest

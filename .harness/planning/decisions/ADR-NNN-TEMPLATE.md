---
adr: ADR-NNN
title: <决策标题>
status: proposed
date: YYYY-MM-DD
deciders: <谁定的：用户 / 架构师 / 用户+AI评审>
authority: <权威来源：哪份文档/哪个结论锁定了本决策>
supersedes: []
superseded_by: ""
gap: ""
blocked_by: ""
depends_on: []
---

# ADR-NNN · <决策标题>

<!-- depends-on: <本 ADR 依赖的其他 ADR 编号，如 ADR-001, ADR-003> -->

> **状态**：<与 frontmatter 一致>
> **日期**：YYYY-MM-DD
> **关联**：<相关文档/代码/其他 ADR>
> **一句话**：<用一句话说清这条决策是什么>

---

## 1. 背景（Context）

### 1.1 问题

<要解决什么问题？当前是怎么做的？>

### 1.2 触发原因

<为什么现在要做这个决定？是踩了坑、架构升级、还是需求变化？>

### 1.3 约束

<有哪些不能违反的约束？（如项目宪法 Principle III、既有铁律、外部依赖限制）>

---

## 2. 决策（Decision）

### 2.1 一句话决策

> **<最精炼的陈述。新 Agent 只看这一句也应该知道该怎么做。>**

### 2.2 细则

| 项 | 规定 |
|---|---|
| | |

### 2.3 执行约束

<怎么保证被遵守？CODE REVIEW 断言？测试？门禁？>

---

## 3. 否决项（Rejected Alternatives）

> **为什么必须写**：不写否决项，未来 Agent 会重新提出同一个被否掉的方案。

### 3.1 <被否决的方案 A>

**否决理由**：<为什么不行>

### 3.2 <被否决的方案 B>

**否决理由**：<为什么不行>

---

## 4. 落地状态（Implementation）

> ⚠️ **本节决定 `status` 字段的取值。**
> - 全部完成 → `status: accepted`，并在下方"已完成"里给出**可核验的证据**（文件:行 / commit / 测试名）
> - 部分完成 → `status: partial`，**必须**在 frontmatter 填 `gap`（还差什么）与 `blocked_by`（为何没做）
> - 未开始 → `status: proposed`（或 accepted 但 gap 明确）

| 步骤 | 内容 | 状态 | 证据 |
|---|---|---|---|
| 1 | | ☐ 待做 / ☑ 已完成 | |

**已完成部分的可核验证据**：
<文件:行号 / commit hash / 测试名 —— 必须是能当场打开看的东西，不能写"已实现"三个字>

---

## 5. 验收标准（Acceptance Criteria）

> 每条必须**可判定**（能给出通过/不通过）。**不做完不勾。**

- [ ] <可判定的标准 1>
- [ ] <可判定的标准 2>

---

## 6. 后果（Consequences）

**正面**：<带来什么好处>

**负面/代价**：<有什么代价、放弃了什么>

**对未来决策的约束**：<它锁死了什么？后继决策不得违反哪一条？>

---

## 7. 变更记录（Changelog）

| 日期 | 变更 | 变更人 |
|---|---|---|
| YYYY-MM-DD | 本 ADR 建立 | |

---

## 8. 被取代时怎么处理（仅在本决策失效时填写）

> 若本决策被后续 ADR 取代，在此处说明：**新决策是哪条、什么变了、哪些旧结论彻底作废**。
> 同时必须在 frontmatter 填 `superseded_by`，并把 `status` 改为 `superseded`。
> **依赖本决策的文档**需要逐个加 STALE 横幅或归档（见协议 §3）。

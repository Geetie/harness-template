{
  "version": 1,
  "description": "harness 钩子声明式配置。默认启用提交闸门（harness 同步 + 反占位符）。",
  "hooks": {
    "pre_commit": [
      {
        "name": "harness-sync",
        "script": ".githooks/pre-commit.py",
        "description": "强制 progress.md 与 lessons.md 与代码同 commit",
        "timeout_seconds": 60
      },
      {
        "name": "no-placeholder",
        "script": "scripts/no_placeholder_guard.py",
        "args": ["--fail-on", "error"],
        "description": "拦截 TODO / 桩实现 / 假数据 / 浅层错误处理",
        "timeout_seconds": 300
      }
    ]
  }
}

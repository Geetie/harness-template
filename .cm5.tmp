docs: 能力清单 —— 把"声称能防的"全部列出来（v1.6.1）

峰宝要求「你自己列出了目前声称能防的东西，全部列出来给我看」。
新增 .harness/CAPABILITIES.md。

做法：**从各脚本代码里抽取**（ast + 正则读 RULES / 检查项），不凭记忆。
覆盖 4 个触发点（提交时 / 交付前 / 日常 / 体检），约 100+ 个检查点：
  pre-commit 5 道 · guard 11 条 · evidence_gate 14 条防线 ·
  check_integration 2 模式 · quality 5 键 · test_audit 8 类 oracle ·
  test_runner 6 能力 · design_smell 5 信号 · harness_lint 11 条 ·
  state_health / init 42 项 / sync_template 4 模式

关键设计：每项都标三件事 —— 防什么 / **前提** / **验证状态**。
验证状态分三级，不混：
  ✅ 双向对照验证过（有正反样本）
  ⚠️ 只跑过整体，未逐条验证（如 guard 的 9/11 条、harness_lint 的 11 条）
  ⚠️ 阈值未校准（smell 的 500 行/20 定义/5 参数是拍定的）

专设 §11「已知的洞」—— 比前面所有节都重要：
  · 假完成（行为对但实现假）：硬编码返回值恰好等于期望 → 通过
  · 未集成的另两种形态：注册不触发 / 写到没人读的地方 → 抓不到
  · HARNESS_SKIP=1 后门：一个环境变量绕过全部 5 道提交检查
  · 非 Python 的证据走静态检查（已标注但确实弱）
  · test_audit 默认不拦（不配 --fail-on 等于没门禁）
  · guard 只见标记不见实质（return 0.0 无标记 → 放过）
  · 证据可被"一起造假"

更根本的一条写进了文档：
**门禁强度 = 它的「不可自证性」。**
凡是 Agent 能自己满足的条件（自写 status / 自写证据 / 自声明 touches），
总能被"满足而不产生实质"。真正硬的只有外部系统观测与独立的人为判断。

§12 给了复验命令与原则：任何"它能拦住 X"的说法都要有正反双向实测；
只有单向验证的结论默认不可信。

CAPABILITIES.md 已注册进 ALWAYS_FILES（元文档，随项目生成）。
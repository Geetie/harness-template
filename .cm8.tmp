fix(init): 补上 config.json 存在性检查 —— 它缺失时多道门禁静默失效（v1.9.0）

第五批 · L008-L011 决策演进（11 项）：全部通过
--------------------------------------------------
用「复制模板 + 基线差值」法逐条质问：
· L008 失效指针：superseded 缺 superseded_by / 单向指针 → 都报；
  双向闭合 → 不报 ✅
· L009 台账一致：正文与台账状态矛盾 / 状态值非法（done）→ 都报；
  一致 → 不报 ✅
· L010 STALE：depends-on 指向失效决策 → 报；指向有效 → 不报；
  **围栏内的 depends-on 示例 → 不报** ✅（这条已正确使用 skip_fence）
· L011 partial 缺 gap / 缺 blocked_by → 都报；写全 → 不报 ✅

这批 11/11，实现是扎实的。

第六批 · L004 / L006 / init（12 项）
--------------------------------------------------
· L004 注册表失配：注册了不存在的卡/技能 → 都报；存在 → 不报 ✅（3/3）
· L006 阅读清单：10 项（阈值 5）→ 报；3 项 → 不报；
  **引用块里的清单 → 不报**；无「强制阅读」标题的普通清单 → 不报 ✅（4/4）

**init 发现真漏报（本轮唯一修复）**：
`load_config` 在 `.harness/config.json` 不存在时返回 `(None, None)`，
main 把它当成"正常默认"继续跑 → init 报"全部通过"。
但 config.json 是多道门禁的开关来源，缺了它：
  · code_root 缺失 → 反占位符检查**跳过**
  · quality 段缺失 → 代码规范检查失败
  · commands 缺失 → 质量命令跳过
→ **多道门禁静默失效，用户看到的是绿灯。**
与 L003（`\b` 导致检查从不触发）同族：**"没配置"与"配置正常"同形。**

修法：新增「③验证层 · config.json 缺失」显式检查项，
并在 detail 里说明后果与重建方式。

验证：有 config → failed=1（仅 hooksPath，复制目录的正常现象）；
无 config → failed=2 且**明确列出 config.json 缺失**，无重复项。
lint 0E/0W · ruff All checks passed。

（过程中我自己踩了一次：插入 main 时重复了一行导致同一项报两次 —— 
所以"修复也要双向验证 + 查重复"。）

fix(guard): 补跨语言「未实现」形态 —— TS/Java/Go 的桩代码此前全部漏过（v1.14.0）

第十一批：非 Python 栈的**行为**验证
--------------------------------------------------
上一批（第十批）只验证了"脚本不崩"，本批造**真该被抓的** TS/Java 占位实现
实测各门禁是否真抓得住。发现两个覆盖缺口。

缺口 ①：`not-implemented` 只认 Python 形态
--------------------------------------------------
实测（规则级双向对照）：

  raise NotImplementedError()                    → 命中 ✅
  throw new Error("not implemented")  (TS/JS)    → **漏过** ❌
  throw new Error("unimplemented")               → **漏过** ❌
  throw new UnsupportedOperationException() (Java) → **漏过** ❌
  panic("not implemented")            (Go)       → **漏过** ❌

本模板支持 python / next-ts / java 三种栈，规则却只覆盖了 Python 的写法。
**用户选 TS 或 Java 栈时，"未实现的桩函数"这一类占位完全抓不到。**

修法：补跨语言形态（同时收紧避免误报）：
  · 词表加 `UnsupportedOperationException`
  · `throw new *(Error|Exception)("<... not implemented | unimplemented | 未实现 ...>")`
  · `panic("<... not implemented | unimplemented | 未实现 ...>")`

双向验证 10/10：
  6 种跨语言形态全部命中 ✅
  4 种不该抓的正确放过 ✅ —— 包括 Python 的 `NotImplemented` 单例（合法用法）、
  普通 `throw new Error("网络超时")`、注释里提及"not implemented"

缺口 ②：`design_smell` 对不支持的栈静默 exit=2
--------------------------------------------------
Java 项目跑它：exit=2，输出只有"没有找到源码文件"——
用户会以为路径写错了，实际是 `.java` 不在支持列表里。

修法：明确说明原因 ——
  「src 下没有找到可分析的源码文件（本目录含 .java，当前仅支持 .py/.ts/.tsx/.js/.jsx）」

（不做 Java 分析支持：嵌套深度需要 AST，成本高于收益；但**不能静默**。）

回归：模板自身 guard exit 0 · smell 27 条信号正常 · lint 0E/0W · ruff All checks passed

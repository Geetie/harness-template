fix(lint): harness_lint 逐条自查 —— 修 4 个真问题（v1.8.0）

峰宝追问「不是约 100+ 检查点吗？怎么就这些？继续修复」。
上一轮只覆盖了约 30 个，harness_lint 的 11 条、init 的 42 项等基本没测。

先修正了测试方法（第一次测错了）
--------------------------------------------------
第一版测试**没控制基线** —— 模板自带文档本身就在报（如 CAPABILITIES.md
有 8 条 L001），我把它们误当成"样本导致的"，得出 6/16 的错误结论。
改用「复制模板 → 跑基线 → 注入样本 → **看差值**」后才看清真相。

发现并修掉 4 个真问题
--------------------------------------------------
1. **L003 完全不工作（最严重，静默失效）**
   `(?:测试|用例|tests?)\b` 里的 `\b` 要求词边界，但中文「测试通过」的
   「试」「通」都是 `\w`，之间**没有**词边界 → `\b` 永远不成立 →
   整条基线漂移检查**从不触发**，而且**不报错**。
   表面看是"没有漂移"，实际是"根本没查"。
   → 改用 `(?![a-zA-Z])`。

2. **L002/L007 不跳围栏**
   代码块里的示例（`` D:/work/proj/... ``、`{{PROJECT_NAME}}`）被当成
   真内容扫描 → 误报。
   → 启用 `iter_lines_skip_fence`。

3. **L001 误报包基建文件名**
   文档里"排除 `__init__.py` / `conftest.py` / `setup.py`"这类**列举**
   被当成文件引用 —— CAPABILITIES.md 的一行就产生 7 条误报。
   → `is_real_path_ref` 增加基建文件名排除（8 条 → 1 条）。

4. **系统性问题：`iter_lines_skip_fence` 只有 L010 用**
   L001/L002/L003/L007 全都没接 —— 文档里"讲解怎么写"的代码块示例
   一律被当内容扫。这是本轮最有价值的发现（一个函数写了但没推广）。
   → 批量接入 4 条规则。

验证（修正版方法 17 项）
--------------------------------------------------
- 围栏内 4 条规则全不报 ✅ / 围栏外同样内容都报 ✅（双向）
- L003 样本能报、模板自身仍 0 报 ✅
- 逐条对照 16/17（剩 1 项是**设计如此**：L001 只认反引号与 Markdown 链接）
- 模板 lint 0E/0W · ruff All checks passed

已记录的已知缺口
--------------------------------------------------
- L001 **裸写路径不检查**（"详见 docs/foo.md" 无标记 → 漏报），
  有意收窄（避免散文噪音），但确实漏一部分真死链
- L005 只认中文「最后更新」，frontmatter 的 `last_updated` 英文 key 不认

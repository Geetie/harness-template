fix(thresholds): 阈值首次用数据校准 —— 噪音降 85%（v1.15.0）

第十二批（最后一批自查）：阈值校准
--------------------------------------------------
所有阈值都是拍定的，从没用数据验证过。本批用**模板自身 16 个脚本**
（真实工程代码）统计实际分布，判据是"只抓最极端的 ~10%"（阈值 ≥ P90）。

发现：两个阈值**严重偏严**，等于在制造噪音
--------------------------------------------------
| 阈值 | 原值 | 样本分布 | 后果 |
|---|---|---|---|
| `MAX_LINES` | 500 | **P50=502**、P90=684 | **8/16 个文件全超限，连中位数都超** → 纯噪音 |
| `MAX_NESTING` | 4 | P90=6 | 7/16 超限。算法计入 if/for/while/try/with，而 Python 里 try 极常见 |

这直接违反 S13「降噪即有效性」——**误报会让人关掉整个检查**。

还有一个更隐蔽的问题：**阈值贴着"当前状态"定**
--------------------------------------------------
`state_health` 的 `AGENTS.md max_lines = 150`，而模板实际**正好 150 行** ——
再改一行就超限。**把"现状"当成"上限"，就无法区分"正常"与"膨胀"**。

修法：
· `MAX_LINES` 500 → **800**（P90=684 之上留余量）
· `MAX_NESTING` 4 → **6**（= P90）
· `AGENTS.md max_lines` 150 → **200**，`max_bytes` 16KB → 24KB（留 ~30% 余量）
· `MAX_TOP_DEFS=20` / `MAX_PARAMS=5` **不改** —— 样本支撑它们是合理的
  （P90=14 仅 1/16 超限；154 个函数 0 超限）

保持不改的项要说明理由，只说"调了哪些"是不够的。

效果
--------------------------------------------------
· 模板自身 smell 信号 **27 条 → 4 条**（噪音降 85%）
· 剩下 4 条是真正的极端：2 个 god-module + 2 个 deep-nesting

双向验证 6/6
--------------------------------------------------
· 700 行文件 → 不报 ✓ / 900 行 → 报 ✓
· 5 层嵌套 → 不报 ✓ / 7 层 → 报 ✓
· AGENTS.md 180 行 → 不报 ✓ / 260 行 → 报 ✓

诚实标注的盲区
--------------------------------------------------
· `MAX_CLASS_METHODS = 15`：**样本无类（最大 2 个方法）→ 校准不了**，
  保留经验值并在代码注释与 CAPABILITIES 里标为"已知盲区"
· 样本仅 16 个文件、风格统一、规模偏小 → 会**低估**真实项目分布，
  新值仍是估计，真实项目的合理值应更高

回归：lint 0E/0W · init 42/42 · ruff All checks passed
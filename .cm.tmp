feat(gate): 证据门禁 —— 让「标完成」必须提供可执行证据（v1.5.0）

起因：红队实验证明现有门禁拦不住垃圾 demo
--------------------------------------------------
构造了符合描述的垃圾 demo（硬编码返回 0.0/{}、未接线、弱测试、自标 completed），
逐个打现有门禁 —— 7 条里只有 3 条拦住，其中 2 条还是歪打正着
（一条报风格问题、一条报测试错误），只有接线检查勉强算相关。

根因：所有门禁都在查「形态」（有没有关键词/文件/接线），没有一条查「行为」。
更致命的是 feature_list 的 evidence 只是字符串字段 ——
legend 写着「由 evidence 指向的真实路径核实」，但从来没有脚本去核实。
于是「我做了」这句话本身就是通过条件。

解法：把 evidence 从「声明」升级为「契约」
--------------------------------------------------
弱（旧）："evidence": "src/cart.py"                只证明文件存在
强（新）："evidence": {
            "cmd": "python scripts/verify/F-001.py",   真的执行
            "expect_exit": 0,
            "expect_stdout": ["总价: 42.0"],           必须含具体数字
            "artifacts": ["out/receipt.json"],         必须存在且非空
            "touches": ["src/cart.py"]                 证据脚本必须真的引用它
          }

三道防线：必须有证据 / 证据必须真跑通 / 证据必须触及实现。
外加强度锁：completed 数与强证据数下降即拦（防悄悄改回 missing 绕过）。

设计目标是「伪造成本 >= 真做成本」：想骗过它得写个能跑出正确值的脚本，
还得真引用被测代码 —— 那不如直接实现。

同时补上「存在但没挂上」的门禁
--------------------------------------------------
- pre-commit 检查 4：证据门禁（仅在 feature_list.json 被改时跑，精准且快）
- pre-commit 检查 5：接线检查（读 config.integration.entry；
  没配入口时明确提示怎么配，不静默跳过）

顺带修
--------------------------------------------------
- guard 的 log-and-rethrow 规则：包装并加上文的重抛被误判为占位式重抛。
  判据改为「raise 后跟标识符+括号+括号内有内容」即视为有上下文
- 模板自身 9 个 ruff lint 问题（3 个 E741、1 个 F541、5 个 F401）——
  此前每个新生成的项目一提交就被自己的质量门禁拦
- 模板仓库补上自己的 config.json（此前它没有 → quality 门禁在模板上必然失效，
  且模板自己过不了自己的门禁）

验证（全部双向对照）
--------------------------------------------------
- 证据门禁 5 场景（无证据拦/弱证据WARN/期望值不符拦/真实现放行/自说自话拦）= 5/5
- 端到端走真实 git commit 3 场景 = 3/3
- guard 规则对照 = 4/4
- 模板 lint 0E/0W；ruff check All checks passed
